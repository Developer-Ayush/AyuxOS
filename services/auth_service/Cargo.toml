[package]
name = "auth_service"
version = "0.1.0"
edition = "2024"

[dependencies]
libaipc = { path = "../../libraries/libaipc" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
argon2 = "0.5"
rand = "0.8"
nix = { version = "0.31.3", features = ["fs"] }
