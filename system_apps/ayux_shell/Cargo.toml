[package]
name = "ayux_shell"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["reboot"] }
termion = "4.0.6"
libaipc = { path = "../../libraries/libaipc" }
libayux = { path = "../../libraries/libayux" }
