[package]
name = "milestone3_tests"
version = "0.1.0"
edition = "2024"

[dependencies]
libaipc = { path = "../../libraries/libaipc" }
libayux_hal = { path = "../../libraries/libayux_hal" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
