[package]
name = "libayux_hal"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "1.0"
nix = { version = "0.31.3", features = ["fs", "ioctl"] }
serde = { version = "1.0", features = ["derive"] }
